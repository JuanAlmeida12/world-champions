import styled from "styled-components";

import PrimaryBG from "assets/bg/bg_primary.jpg";

export const AppContainer = styled.div`
  background-image: url(${PrimaryBG});
  width: 100vw;
  height: 100vh;
`;
